/* (IZ 2006-08-13) {
Synthdefs for copying the signal of one bus to another.
Needed to realise n-to-n connections between synths (or scripts).
} */

[
SynthDef('kr_bus_copy', { | in, out | Out.kr(out, In.kr(in)) }),
SynthDef('ar_bus_copy', { | in, out | Out.ar(out, In.ar(in)) }),
]

/* IZ 2007-01-27 { SC3

To include another script in a script, use the following code: 

~script.include('ScriptFolderAsSymbol', 'ScriptNameAsSymbol');

The present file is also an example of the include mechanism. Press the [Enter] key to test this script. 

} */

// Include an example pattern script: 
~script.include('About Scripts', '08 pattern');

// Start the default server for playing the script: 
if (Server.default.serverRunning.not) { Server.default.boot };

